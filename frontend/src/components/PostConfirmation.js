import React from "react";
import styled from "styled-components";

const PostConfirmation = () => {
    return <Wrapper>Your ride is now listed</Wrapper>;
};

export default PostConfirmation;

const Wrapper = styled.div``;
