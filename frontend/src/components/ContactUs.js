// IMPORT DEPENDENCIES
import React from "react";
import styled from "styled-components";

// IMPORT COMPONENTS

const ContactUs = () => {
    return <Wrapper>Contact Us</Wrapper>;
};

export default ContactUs;

const Wrapper = styled.div`
    padding: var(--padding-page);
    font-size: 50px;
`;
